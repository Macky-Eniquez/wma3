<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LABQUIZ_WMA3</title>
    <script type="module" src="https://cdn.jsdelivr.net/npm/@ionic/core/dist/ionic/ionic.esm.js"></script>
    <script nomodule src="https://cdn.jsdelivr.net/npm/@ionic/core/dist/ionic/ionic.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@ionic/core/css/ionic.bundle.css" />
</head>
<body>

    <ion-app>
        <!-- HEADER -->
        <ion-header>
            <ion-toolbar color="dark"> <!-- primary, secondary, tertiary, medium, dark -->
                <ion-title>HEADER TITLE</ion-title>
            </ion-toolbar>
        </ion-header>

        <ion-content>
            <!-- 1st item DISTANCE -->
            <ion-item> 
                <ion-label position="floating">Distance: </ion-label><br>
                <ion-input id="distance" 
                label="Enter distance" 
                label-placement="floating" 
                type="number"></ion-input>
            </ion-item>

            <!-- 2nd item RATE-->
            <ion-item>
                <ion-label position="floating">Rate per km: </ion-label><br>
                <ion-input id="rate"
                label="Enter rate per km"
                label-placement="floating"
                type="number"></ion-input>
            </ion-item>

            <!-- 3rd item TAX-->
            <ion-item>
                <ion-label position="floating">Tax (%): </ion-label><br>
                <ion-input id="tax"
                label="Enter tax"
                label-placement="floating"
                type="number"></ion-input>
            </ion-item>

            <!-- button -->
            <ion-button expand="block" 
            color="success"
            onclick="calculateTotalFare()">PROCESS OPERATION <ion-icon name="checkmark-circle"></ion-icon></ion-button>

            <!-- last item TOTAL FARE -->
            <ion-item>
                <ion-label>Total Fare: </ion-label>
                <ion-text id="total"></ion-text>
            </ion-item>

        </ion-content>

    </ion-app>
    
<!-- <script src="main1.js"></script> -->
 <script>
    function calculateTotalFare() {
      const distance = parseFloat(document.getElementById("distance").value);
      const rate = parseFloat(document.getElementById("rate").value);
      const tax = parseFloat(document.getElementById("tax").value);


      const total = distance * rate + (distance * rate * tax) / 100;
      //document.getElementById("total").textContent = '${total.toFixed(2)}pesos';
      document.getElementById('total').innerHTML = ${total} pesos
}      
 </script>

</body>
</html>
cdn.jsdelivr.net
